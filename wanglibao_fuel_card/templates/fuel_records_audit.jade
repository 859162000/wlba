doctype html
html
  head
    title 购买记录
    include include/fuel_head.jade
  body
    nav.records-nav
      ul
        if status == 'auditing'
          li.active
            a(href="/fuel_card/buy-record/auditing/") 审核中
          li
            a(href="/fuel_card/buy-record/done/") 已完成
        else
          li
            a(href="/fuel_card/buy-record/auditing/") 审核中
          li.active
            a(href="/fuel_card/buy-record/done/") 已完成


    .records-item
      if data
        if status == 'auditing'
          redpackname  = 'records-list-blue'
        else
          redpackname  = 'records-list-grey'
        for list in data
          .records-list(class='{{redpackname}}')
            .records-card-amount
              principal = int(list.limit_min_per_user)
              span.amount {{principal}}
              |元
            .records-card-detail
              .records-name {{list.product_amortization.product.name}}
              .records-dec
                equality = int(list.equality_prize_amount)
                span.records-propress-text 已得{{equality}}元理财加油卡
                span.records-propress-num {{list.term}}/{{list.product_amortization.product.amortization_count}}
              .records-date {{list.term_date | date:"Y-m-d"}}

      else
        .records-default
          img(src='/static/imgs/mobile_fuelcard/public/default.png')
          p 暂无记录，快去投资吧！

