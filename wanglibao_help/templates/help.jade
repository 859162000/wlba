-load formatters
-load mathfilters
doctype html
html
  head
    - with '安全保障' as title
    include include/common_head
    - endwith
    meta(name="keywords", content="安全保障")
    meta(name="description", content="没有最安全，只有更安全")
  body.white-background
    include include/header
    - with '安全保障' as activeItem
    include include/navigation
    - endwith

      .container.help
        .sub-nav
          span 首页
          span &gt;
          span 帮助中心
          span &gt;
          span 注册登录

        .help-menu
          ul
            //li
            //  a(href="#") 热门问题
            //li.current
            //  a(href="#") 注册登录
            //li
            //  a(href="#") P2P
            //li
            //  a(href="#") 货币基金
            //li.last
            //  a(href="#") 安全保障
            for item in nav
              li(data-target="{{item.title}}")
                a(href="#") {{item.title}}
        .help-content
          for key,value in questions.items
            if key == '热门问题'
              .hot-items.help-box(data-source="{{key}}")
                h1 {{key}}
                ul
                  for item in value
                    li
                      a(href="") {{item.title}}
            else
              .list-items.help-box(data-source="{{key}}")
                h1 {{key}}
                .list-container
                  for item in value
                    .list-item
                      .list-item-title {{item.title}}
                      .list-item-content {{item.answer}}

    script.
        require(["/static/js/help.js"])
    - with 1 as no_footbar
    include include/footer
    - endwith
    include include/tracking
