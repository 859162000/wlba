- load formatters
doctype html
head
    - with '查询用户充值记录' as title
    include include/common_head
    - endwith
    body
        .form-row
            a(href="/admin")
                button.button-home-action(type='submit') 返回
        form(method="GET" action='/admin/pay/transaction/withdraw').form
            - csrf_token
            .form-row
                label 请输入用户手机号
                .row
                    input(name="phone")
            .form-row
                button.button-primary(type='submit', value='查询') 查询