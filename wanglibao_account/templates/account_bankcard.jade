extends account_home_base

-load formatters

block head
  - with '我的银行卡' as title
    include include/head

block sidebar
  - with '银行卡管理' as activeItem
    include include/user_center_sidebar

block main
  .panel-padding.clearfix
    .bank-card-manage 银行卡管理
    .bank-card-manage(style='display:none') 绑定银行卡
        span 绑定的银行卡持卡人，需同身份认证的个人信息保持一致
    .banks-list
        .manage-pop 为了您的资金安全，网利宝实行了新的
            a 银行卡管理制度
            | ，请您通过网页版再次对APP已绑定银行卡进行认证。
        .manage-pop 已经在APP绑定银行卡的用户，仍可通过APP进行充值、提现等操作。
        .row.padding-l-r-m
          if p2p_cards
            each card in p2p_cards
              .bank-card
                .bank-card--title-bar.row
                  .bank-card--bank-name.span9 {{card.bank.name}}
                      span 待绑定
                  .bank-card--icon.span3-omega
                    img(src='/static/images/bank_card.png')
                .row
                  dl.bank-card--info-row
                    dt.bank-card--info-title 账号
                    dd.bank-card--info-value {{card.no | bank_card}}
                .binding-card 绑定该卡
                  //a#del-card.pull-right.del-bank-card(card_id='{{card.id}}') 删除

            a#add-card-button(href='#add-card-form')
              .add-bank-card
                .add-icon-large
                p.add-card-text 添加一张银行卡
          else
            a#add-card-button(href='#add-card-form')
              .add-bank-card
                .add-icon-large
                p.add-card-text 添加一张银行卡
        .kindly-reminder
            p
                b 温馨提示
            p 1. 绑卡成功后，将自动为您开通快捷支付功能，方便您进行快速充值。
            p 2. 绑卡成功后，您的提现资金将统一打到该银行卡。
            p 3. 出于账号安全性考虑，每个账户，只能绑定一张银行卡，实行同卡进出原则。
            p 4. 如需取消绑卡或者更换银行卡，可通过线上申请，或联系客服4008-588-066进行咨询。

      include include/add_card
  script.
    require(['/static/js/add_card.js'])