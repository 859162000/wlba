-load formatters
doctype html
html
    head
        include include/weixin_head.jade
    body
        header
            hgroup 实名认证
            a(href="javascript:history.back()")
                span 返回
        .authentication-from-error
        .authentication-from-one
            label 姓&nbsp;&nbsp;&nbsp;名
            input(type="text", name="name", placeholder="请输入真实姓名").check-input
        .authentication-from-error
            .error-name.sign-all(style="display: none;")
        .authentication-from-two
            label 身份证
            input(type="text", name="id_number", placeholder="请输入身份证号").check-input
        .authentication-from-error
            .error-card.sign-all(style="display: none;")
    .authentication-from-four
            a(href="#").from-four-complete 完成
    script(data-init='authentication', src="/static/scripts/mobile/dist/mobile.js")
